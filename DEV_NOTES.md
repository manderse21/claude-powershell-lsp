# Moved

This file moved to **[docs/DEV_NOTES.md](docs/DEV_NOTES.md)** (dispatch 000250) -- the developer
notes are a contributor reference, and `docs/` is where this repository keeps contributor
references. Nothing was dropped in the move.

This stub stays so that links written before the move -- in dispatches, issues, pull requests, and
external posts -- still resolve. New links should point at `docs/DEV_NOTES.md`.
